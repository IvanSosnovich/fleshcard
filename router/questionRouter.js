const { Router } = require("express");
const User = require('..')
const Question = require('..')
const router = express.Router()

const router = Router();

router.post('/', (req, res) => {
  const {} = req.body

})

module.exports = router;
